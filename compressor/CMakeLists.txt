set(
  XRAY_COMPRESSOR_DIR ${CMAKE_SOURCE_DIR}/compressor
  CACHE PATH
  "X-Ray Compressor Directory"
)

set(XRAY_COMPRESSOR xrCompress.exe CACHE PATH "X-Ray Compressor")
set(XRAY_COMPRESSOR_LOG engine.log CACHE PATH "X-Ray Compressor Log")
